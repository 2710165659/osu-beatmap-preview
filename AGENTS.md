# 项目开发规范

本文件适用于 `osu-beatmap-preview` 项目根目录下的全部代码。后续修改应遵守以下约定。

## 测试代码

- 测试代码不得参与生产构建。
- 内联测试模块必须使用 `#[cfg(test)] mod tests` 或等价的测试模块名。
- 测试专用函数、测试专用实现块、测试专用常量和测试专用导入也必须使用 `#[cfg(test)]` 保护。
- 独立测试源文件必须在文件顶部使用 `#![cfg(test)]`，并且只能通过 `#[cfg(test)] mod ...;` 引入。
- 每个测试函数必须使用 `#[test]`、`#[tokio::test]` 等测试属性。
- 测试函数和测试辅助代码统一放在对应源文件末尾；正常函数和测试函数不得交错排列。
- 测试应放在被测模块对应的文件中，不得为了复用测试入口而堆积到 `main.rs`。
- 不得在生产入口中保留日志测试、模拟请求或其他隐藏测试入口。

## 注释与文档

- 所有叙述性注释、文档注释和模块说明使用中文。
- API 名称、协议名称、库名称、代码标识符、公式、文件格式字段和必要的英文专有名词可以保留原文，但不得用英文句子替代中文说明。
- 复杂函数、非直观算法、边界条件、缓存策略、并发逻辑、FFI 调用、编码封装和兼容性处理必须补充简洁的中文注释。
- 注释应说明“为什么这样做”、重要不变量和边界行为，避免逐行翻译代码或添加无意义的说明。
- 修改逻辑时同步检查附近注释是否仍然准确，禁止保留与实现不符的注释。

## 标准库写法与可读性

- 优先使用语义明确的标准库 API，例如 `clamp`、`contains`、`div_ceil`、`is_multiple_of`、`repeat_n`、`HashMap::entry`、`is_none_or` 和 `?`。
- 避免多余借用、显式生命周期、重复类型转换、无效的 `clone`、手动循环计数器和不必要的控制流包装。
- 使用这些 API 前必须确认边界和浮点 NaN 行为不会改变业务语义；若旧行为有特殊回退，使用显式分支保留该行为并加中文注释。
- 不要为了消除单条 lint 警告而进行无关的接口重构或改变渲染结果。

## 渲染缩放

- 新增或修改任何画面绘制内容时，必须将文字、图形、精灵、线宽、尺寸、间距、边距和定位偏移等全部接入对应模式与输出格式的 `SCALE`，在底层按倍率计算实际绘制参数；禁止遗留固定像素尺寸、读取其他格式的布局配置，或在渲染完成后整体缩放成目标尺寸。
